# Two ways to run this:
#
#   docker compose up
#       The app alone. Point it at an Ollama you already run on the host, or at any
#       OpenAI-compatible endpoint, from the Settings page.
#
#   docker compose --profile with-ollama up
#       The app plus its own Ollama container. Self-contained, but you must pull a model
#       into it once:  docker compose exec ollama ollama pull qwen3:8b

services:
  app:
    build: .
    image: teacher-assistant:latest
    ports:
      - "8000:8000"
    volumes:
      # Everything the teacher owns — uploads, parsed text, the search index, exports and
      # the database — lives here. Back this up; the rest is disposable.
      - teacher-data:/data
    environment:
      TA_DATA_DIR: /data
      TA_DATABASE_URL: sqlite+aiosqlite:////data/teacher_assistant.db
      TA_LLM_PROVIDER: ${TA_LLM_PROVIDER:-ollama}
      # host.docker.internal reaches an Ollama running on the host machine.
      TA_LLM_BASE_URL: ${TA_LLM_BASE_URL:-http://host.docker.internal:11434}
      TA_LLM_MODEL: ${TA_LLM_MODEL:-qwen3:8b}
      TA_LLM_TIMEOUT: ${TA_LLM_TIMEOUT:-900}
      TA_WORKER_CONCURRENCY: ${TA_WORKER_CONCURRENCY:-2}
      TA_CORS_ORIGINS: http://localhost:8000
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped

  ollama:
    profiles: ["with-ollama"]
    image: ollama/ollama:latest
    ports:
      - "11434:11434"
    volumes:
      - ollama-models:/root/.ollama
    restart: unless-stopped
    # Uncomment to use an NVIDIA GPU — roughly ten times faster than CPU for generation.
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: all
    #           capabilities: [gpu]

volumes:
  teacher-data:
  ollama-models:
